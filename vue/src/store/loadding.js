const state = {
    isLoaddingApp:false
}

const actions = {
   
}

const mutations = {
    
}

const getters= {
     isLoaddingApp(state){
         return state.isLoaddingApp
     }
}

export default {
    state,
    actions,
    mutations,
    getters
}